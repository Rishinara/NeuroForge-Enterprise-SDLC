# ===================================================================
# Spring Profile: prod (Strict Production Profile)
# ===================================================================

# Database (Strict validation, environment injection required)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/neuroforge_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Root}
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Security
jwt.secret=${JWT_SECRET:jt58MFja6NWfCjFyWVGsK9Egwr2pKj3xHeNJxLW6DTc=}

# Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SMTP_USERNAME:vmowneesh8@gmail.com}
spring.mail.password=${SMTP_PASSWORD:drorbsqkgnplwbue}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Groq AI Service Configuration
groq.api.key=${GROQ_API_KEY:mock_groq_api_key}
