# ===================================================================
# Spring Profile: dev (Default Development Profile)
# ===================================================================

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/neurofourge_db
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true


# Security (Safe fallback key for local dev)
jwt.secret=${JWT_SECRET:dev_secret_key_must_be_at_least_256_bits_long_for_hs256_algorithm}

# Mail Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SMTP_USERNAME:vmowneesh8@gmail.com}
spring.mail.password=${SMTP_PASSWORD:drorbsqkgnplwbue}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Groq AI Service Configuration
groq.api.key=${GROQ_API_KEY:mock_groq_api_key}

# Dev Logging
logging.level.root=INFO
logging.level.com.neuroforge=DEBUG

