package com.neuroforge.enums;

public enum Role {
    SUPER_ADMIN,
    ORG_ADMIN,
    PROJECT_MANAGER,
    DEVELOPER,
    QA_TESTER,
    CLIENT
    // ROLE_USER and ROLE_ADMIN removed — legacy V1 values
}